{
  "runner": "import os\nimport tempfile\n\nimport pandas as pd\nimport polars as pl\n\nscore = 0.0\nnamespace = {}\nexec(CODE_SOURCE, namespace)\nfn = namespace['process_sales']\n\ndf = pd.DataFrame({\n    'region': [' north ', 'south', 'north', 'south', 'north'],\n    'status': ['ok', 'ok', 'cancelled', 'ok', 'ok'],\n    'revenue': [100, 200, 300, 400, 500],\n    'cost': [50, None, 150, 200, None],\n    'order_date': pd.date_range('2023-01-01', periods=5)\n})\n\nfd, csv_path = tempfile.mkstemp(suffix='.csv')\nos.close(fd)\ndf.to_csv(csv_path, index=False)\n\ntry:\n    result = fn(csv_path, exclude_regions=['south'])\n\n    if isinstance(result, pl.DataFrame):\n        columns = result.columns\n        rows = result.to_dicts()\n        regions = [str(row['region']).lower() for row in rows]\n        revenue_values = [row['revenue_sum'] for row in rows]\n        margin_values = [row['margin_mean'] for row in rows]\n    else:\n        columns = list(result.columns)\n        regions = [str(value).lower() for value in result['region'].tolist()]\n        revenue_values = result['revenue_sum'].tolist()\n        margin_values = result['margin_mean'].tolist()\n\n    if len(revenue_values) > 0:\n        score += 0.15\n    if 'revenue_sum' in columns:\n        score += 0.15\n    if 'rolling_rev_mean' in columns:\n        score += 0.15\n    if not any('south' in region for region in regions):\n        score += 0.20\n    if revenue_values and max(revenue_values) > 0:\n        score += 0.15\n    if margin_values and all(value <= 1 for value in margin_values):\n        score += 0.20\n\nfinally:\n    if os.path.exists(csv_path):\n        os.unlink(csv_path)\n\nif 'scan_csv' in CODE_SOURCE or 'LazyFrame' in CODE_SOURCE:\n    score = max(score, score)\nif 'group_by' not in CODE_SOURCE:\n    score = min(score, 0.65)\nif 'with_columns' not in CODE_SOURCE:\n    score = min(score, 0.65)\nif '.collect()' not in CODE_SOURCE:\n    score = min(score, 0.65)\nif 'import pandas' in CODE_SOURCE:\n    score = min(score, 0.20)\n\nprint(f'SCORE:{score:.2f}')\nif score < 0.75:\n    raise AssertionError(f'Insufficient hard-task score: {score:.2f}')"
}
